# Fangorn Agent Web Dev Panel

This web-app is run when `USE_WEB=true` in the `run.sh` file. It allows for natural language interactions and can be used to test different functionalities with the agent.